"""Generated by prompiler.codegen — do not edit by hand.

Vendored standalone artefact. Imports only stdlib and ``pydantic``;
no ``prompiler`` runtime dependency. ``COMPILER_PROTOCOL_VERSION`` and
``SPEC_HASH`` pin the protocol + spec contents so drift between this
vendored copy and the live spec is detectable.
"""

import datetime as _dt
from decimal import Decimal
from typing import Annotated, Literal

from pydantic import BaseModel, ConfigDict, Field

COMPILER_PROTOCOL_VERSION = "{{ protocol_version }}"
SPEC_HASH = "{{ spec_hash }}"


{{ body }}
